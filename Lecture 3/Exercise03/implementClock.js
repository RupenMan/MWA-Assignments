/**
 * Written on April 4, 2018
 * Implements the clock module to display woohoo every second
 * @author Rupendra MAHARJAN
 */
const Clock = require('./clock');
const runClock = new Clock();
runClock.tick();